# Copy to .env and adjust as needed. Never commit a real .env.
# All values are optional this phase; sensible defaults are applied in app/core/config.py.

# Application metadata
APP_TITLE="Document Intelligence API"
APP_ENV=local

# --- Seams for future phases (declared here, not yet wired into runtime) ---
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/document_intelligence
# REDIS_URL=redis://localhost:6379/0
